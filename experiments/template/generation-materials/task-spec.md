# <experiment_title> 課題仕様

## 目的

<task_summary>

## 入力

- 対象 workbook:
- 対象 sheet:
- 参照する日次/明細データ:
- キー列:
- 転記/集計対象列:

## 出力

- 更新する workbook:
- 更新する sheet:
- 更新する列:
- 更新してはいけない列:

## 処理要件

- キー照合は行順に依存しない。
- 既存値をどう扱うかを明記する。
- 同じ workbook セッションで再実行しても結果が壊れないようにする。
- 対象外データ、欠損、重複、非数値などを扱う場合は期待動作を書く。
- 外部通信、固定実パス、追加参照は原則使わない。

## 入口プロシージャ

- 希望する入口名:
- 引数:
- 実行前提:

入口名を指定しない場合、評価時は `final-output.md` または `.bas` 内の明示を優先します。明示がない場合は、単一の引数なし `Public Sub` のみ実行候補にします。

## 完了条件

- `.bas` と説明 Markdown が保存されている。
- 実行手順が公開可能な情報だけで説明されている。
- 検証用 workbook や scratch `.xlsm` を成果物にしていない。
